# Problems

## Merge sort
- MergeSort() divide every array until remain only one item per array (recursively) and calls merge
- Merge() create 2 aux arr, put the first halve in the first aux arr and so on, after that, solve the arr putting then together in the original arr, after that put the remain numbers    